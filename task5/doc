Скрипцов Дмитрий 401 группа

Ostream_iter
Работает исправно

mergesort
Проходит компиляцию, однако работает с ошибками (в основном, дублирует последние числа массива).
Например, массив {2, 1, 4, 3} сортирует неправильно, дублируя 3 в последний слот.
Ошибка заключается в неверной реализации алгоритма выбора нужного числа при рассмотрении массива, в следствие чего
временный вектор переходит из ситуации 1-2-3-0 в ситуацию 1-2-3-3 (в конце согласно построенному алгоритму происходит выбор между
3 и 4 в пользу меньшего).
На данный момент универсальное решение не найдено.
